node_modules/
dist/
artifacts/
.gjc/

# Generated motion archives are large binaries produced by the remote box, not
# source. Keep only the explicitly listed demo and test fixtures below.
*.npz
tools/ardy/out/
# Exceptions: the hosted demo seed clip ships with the build; test-only clips
# stay tracked as fixtures but are never copied into production output.
!public/demo/*.npz
!test/fixtures/qa-lying.npz

.DS_Store
*.log
__pycache__/
*.pyc

# MCP server local install
mcp/node_modules/
.omo/ulw-loop/

# agent scratch
.omo/
