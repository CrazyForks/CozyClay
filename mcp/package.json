{
	"name": "@cozyclay/mcp",
	"version": "0.1.0",
	"private": true,
	"description": "MCP server for CozyClay — block a scene, place the camera, and get film vocabulary and AI prompts back.",
	"type": "module",
	"bin": {
		"cozyclay-mcp": "server.mjs"
	},
	"scripts": {
		"start": "node server.mjs",
		"verify": "node verify.mjs",
		"verify:prompts": "node verify-prompts.mjs",
		"verify:live": "node verify-live.mjs && node verify-live-p0.mjs && node verify-live-batch.mjs && node verify-live-editor-model.mjs && node verify-live-port.mjs"
	},
	"engines": {
		"node": ">=22"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.30.0",
		"ws": "^8.19.0",
		"zod": "^3.25.0"
	}
}
