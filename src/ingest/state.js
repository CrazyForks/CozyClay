// Phase-4 placeholder for the ingest surface's shared state. This module is the
// resolvable src/ingest target the build-exclusion negative control injects into
// the app entry (plan 6.1), so it must stay importable and trivially small until
// the real surface lands.
export const SURFACE_STAGE = "placeholder";
